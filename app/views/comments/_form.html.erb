<section class="container flex-column d-flex justify-content-center pt-3 pb-5 text-center">

  <%= render 'common/errors', model: @comment %>

    <% if user_signed_in? %>

      <%= form_for [@article, @comment], url: article_comments_path(@article, @comment) do |f| %>

        <div class="form-group">
          <%= f.label :komentarz %><br>
          <%= f.text_area :body %>
        </div>

        <div class="form-group pb-5 correct">
          <%= f.submit 'Utwórz', class: 'btn btn-primary new_submit'%>
        </div>
			  
      <% end %>

  <% end %>
  
</section>